# B&A Command Center — standalone canónico. El build copia los estáticos a dist/.
[build]
  command = "rm -rf dist && mkdir -p dist && cp index.html tweaks-panel.jsx dist/ && cp -r css js dist/"
  publish = "dist"

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache"
